<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!--
    Este projeto e o interior do hexagono.
    Referencia apenas o dominio (Core) e NENHUM PackageReference.
    Se um pacote de persistencia aparecer aqui, a regra de dependencia foi violada.
  -->
  <ItemGroup>
    <ProjectReference Include="..\Core\Core.csproj" />
  </ItemGroup>

</Project>
